/*const myIndex = require("./index");

it("Should return boardStatus equal to No moves played", () => {
  expect(myIndex().toBe("No moves have been played"));
});

/*
describe('Just open Google front page', function(){
  it('works', function(done) {
    router.get('localhost:8080/api/boardStatus')
      .expect(201)
      .expect(function(res) {
        if (true) return 'should not this fail?!?';
      })
      .end(function(err, res){
        done();
      });
  });
});
*//*
it("adsfasdfa", () => {
  expect(logic.returnBoard()).toMatc
});*/
